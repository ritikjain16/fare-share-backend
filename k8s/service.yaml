kind: Service
apiVersion: v1
metadata:
  name: fareshare-backend-service
  namespace: fareshare
spec:
  selector:
    app: fareshare-backend
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
